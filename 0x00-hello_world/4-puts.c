#include <stdio.h>
/**
 * main - A program that prints then followed by a line with puts function
 * Return: 0
 */
int main(void)
{
puts("programming is like building a multilingual puzzle);
return (0)
}
